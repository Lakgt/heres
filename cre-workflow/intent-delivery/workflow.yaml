# ==========================================================================
# CRE WORKFLOW SETTINGS FILE — Heres Protocol Intent Delivery
# ==========================================================================

staging-settings:
  user-workflow:
    workflow-name: "heres-intent-delivery-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config/config.staging.json"
    secrets-path: "../secrets.yaml"

# ==========================================================================
production-settings:
  user-workflow:
    workflow-name: "heres-intent-delivery-production"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config/config.production.json"
    secrets-path: "../secrets.yaml"
